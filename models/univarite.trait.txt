model{
  
  # Model structure

  for(j in 1:n_traits){
    for(i in 1:n_obvs){
      obvs[i,j] ~ dnorm(mu_trait[j], tau_obvs[j])
    }
  }
  
  # Priors
  
  for(j in 1:n_traits){
    mu_trait[j] ~ dnorm(0, 0.005)
    tau_obvs[j] ~ dgamma(0.005, 0.005)
    sigma2_obvs[j] <- 1/tau_obvs[j]
  }

  
}
