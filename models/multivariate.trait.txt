model{
  
  # Model structure
  
  for(j in 1:n_traits){
    for(i in 1:n_obvs){
      obvs[i,j]~dnorm(mu_trait[j], tau_obvs)
    }
  }
  
  # Priors
  
  mu_trait[1:n_traits]~dmnorm(mu0[],Sigma0)
  tau_obvs~dgamma(0.001,0.001)
  
}